   
   1  新添加 ztSuperWorkspace
      先重载原来的方法， open , opendatasource 等。
   
      open 从数据库中读 dataSource 的信息，打开所有 isautoload 的。
      openDataSource,  给数据库 datasource 中添加一条记录，并且打开。
      closeDatasource。
      
   2  给supermap 添加从数据库中打开的方法。
      
      
      
   080115 讨论：
      主题：关于 supermap 的工作空间存储，操作问题。
            现在我们都是采用 smw 文件存储的方式。在实际应用中发现了一些问题。
            我做了一个尝试，自己做一个操作工作空间的方法，把工作空间的信息保存在数据库中。
      起因：
         1 遥感集成中一个工作空间保存了20 多个 oracle 数据连接，1600 多张地图，现在打开工作空间速度很慢。
         2 二调中同时只能打开一个工作空间，不利于汇总操作。
         3 地图的加载，是以数据源别名匹配，地图不能通用。
         
      尝试：
		 1 地图是可以保存在数据库中的，这个现在已经可以用了。
		 2 分析 sxw 文件内容。
		 3 设计自己的 ztSuperWorkspace 类，数据源信息保存在数据库中。

      问题：
	     1 实际意义到底有多大。
	     2 还有没有更好的办法。
	     
	     
	     
	     他妈的，创建数据集和数据源的界面 listview 的图标总是出不来，有时候能出来，弄半天。窝火。